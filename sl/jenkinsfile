@Library('sample-library') _
pipeline {
    agent any
    stages {
        stage ('Example') {
            steps {
                helloWorld(dayOfWeek:"thursday", name: "shakeer")
                }
            }
        }
    }
